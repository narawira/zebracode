{
  "name": "zebra-code",
  "version": "1.0.0",
  "description": "An easy-to-use Barcode & QR generation service that supports various popular barcode types.",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "typescript": "*"
  }
}